name: Model Training Pipeline

on:
  push:
    branches:
      - dev
jobs:
  deploy:
    name: Build & Deploy Skybound (Hostinger VPS)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Add SSH key
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Deploy to Hostinger VPS
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
            set -e

            cd Skybound-backend

            # Pull latest code
            if [ -d ".git" ]; then
              git fetch origin
              git checkout dev
              git reset --hard origin/dev
              git clean -fd
            else
              git clone -b dev https://github.com/edquestofficial/skybound-backend.git .
            fi

            # Install uv if missing
            if ! command -v uv &> /dev/null; then
              curl -LsSf https://astral.sh/uv/install.sh | sh
            fi

            export PATH="$HOME/.cache/uv/bin:$PATH"

            # Setup virtualenv
            uv venv .venv
            source .venv/bin/activate
            uv sync

            # Restart FastAPI service
            sudo systemctl restart fastapi
          EOF

      - name: Notify success
        run: echo "Skybound FastAPI deployed for dev env successfully on Hostinger VPS 🚀"
