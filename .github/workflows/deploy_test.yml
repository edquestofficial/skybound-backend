name: Model Training Pipeline

on:
  push:
    branches:
      - yogeshbhatt

jobs:
  deploy:
    name: Build & Deploy Skybound
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Add SSH key
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.EC2_KEY }}

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
            cd ~/skybound-backend

            # Pull latest code
            git pull origin main || git clone https://github.com/edquestofficial/skybound-backend.git .
            git checkout revamp-be

            curl -LsSf https://astral.sh/uv/install.sh | sh
            echo "$HOME/.cache/uv/bin" >> $GITHUB_PATH

            # Create venv if not exists
            uv venv .venv
            source .venv/bin/activate
            uv sync

            # Restart backend
            sudo systemctl restart fastapi
          EOF

      - name: Notify success
        run: echo "AWS Lambda Function deployed successfully!"
